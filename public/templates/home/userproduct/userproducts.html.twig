
{% extends 'frontbase.html.twig' %}
{% block title %}Shopcart{% endblock %}

{% block content %}

    {{ include ('admin/messages.html.twig') }}


<style>
.ring-in {

    position: relative;
    width: 25%;

}

a:hover, a:focus {

    color: white !important;
    text-decoration: none;

}

a.at-in {

    border: none;
    padding: none;
    margin-left: 11%;
    display: contents;
}
.img-responsive, .thumbnail > img, .thumbnail a > img, .carousel-inner > .item > img, .carousel-inner > .item > a > img {

    max-height: 152.9px;
    object-fit: scale-down;
    margin: auto;
}
.t-head {text-align: center;}
 </style>

{{ include ('home/sidebar2.html.twig') }}
 

	<!-- contact -->
		<div class="check-out">	 
		<div class="container" style="margin-top: 50px;" dir="ltr">	 
	 <div class="spec ">
				<h3>قائمة المنتجات</h3>
					<div class="ser-t">
						<b></b>
						<span><i></i></span>
						<b class="line"></b>
					</div>
			</div>
		

 
 
      <button style="float: right; margin-bottom: 20px;"type="submit" class="my-cart-btn my-cart-b" class="btn"><a href="{{ path('product_new') }}"><span class="glyphicon glyphicon-plus-sign"></span> إضافة منتج</a></button>
 <table class="table " style="border-bottom: none;border-left: none;text-align: center;">
		  <tr>
			<th class="t-head head-it ">المنتجات</th>
            <th class="t-head">اسم المنتج</th>
			<th class="t-head">السعر</th>
		<th class="t-head">الكمية</th>
      


			{# <th class="t-head">Purchase</th> #}
		  </tr>
          
            {% for product in products %}
		  <tr class="cross">
			<td class="ring-in t-data">
				<a href="single.html" class="at-in">
					<img src="{{ asset('uploads') }}/images/{{ product.image }}" class="img-responsive" alt="">
				</a>

				<div class="clearfix"> </div>
               
				    {{ include('home/userproduct/_delete_form.html.twig') }}
			 </td>
             <td  class="t-data"> <h5>{{ product.title }}</h5> </td>
			<td class="t-data" dir="rtl">{{ product.sellprice }} ريال سعودي</td>
			<td class="t-data">
										<div >{{ product.amount }}</div>									
							
								
							</div>
			
			</td> </tr>
			       {% else %}
                <tr>
                    <td colspan="5"><h4 style="justify-content: center;display: flex;">ليس لديك منتجات بعد</h4></td>
                </tr>


			{% endfor %}

            

            {{ include ('admin/messages.html.twig') }}
	</table>
		 </div>
		 </div>

			


{% endblock %}

